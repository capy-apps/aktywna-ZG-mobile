import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.aktywnazg',
  appName: 'aktywna-ZG',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
